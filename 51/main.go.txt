package main

import (
	"context"
	"log"
	"os"

	"github.com/elastic/go-elasticsearch/v9"
	"github.com/joho/godotenv"
)

func main() {
	if err := godotenv.Load(); err != nil {
		log.Fatal(err)
	}
	inport := os.Getenv("IN_ES")
	// fmt.Println(inport)

	thisctx := context.Background()
	client, err := elasticsearch.NewTyped(
		elasticsearch.WithAddresses("http://localhost:"+inport),
		elasticsearch.WithBasicAuth("elastic", "123456"),
		// see : https://www.elastic.co/docs/reference/elasticsearch/clients/go/advanced/observability#_debug_logging

		// elasticsearch.WithLogger(&elastictransport.ColorLogger{
		// 	Output:             os.Stdout,
		// 	EnableRequestBody:  true,
		// 	EnableResponseBody: true,
		// }),

		// elasticsearch.WithLogger(&elastictransport.JSONLogger{
		// 	Output:             os.Stdout,
		// 	EnableRequestBody:  true,
		// 	EnableResponseBody: true,
		// }),
	)
	if err != nil {
		log.Fatal(err)
	}
	defer client.Close(thisctx)

	// res, err := client.Search().
	// 	Index("products").
	// 	Query(esdsl.NewMatchQuery("name", "pasta")).
	// 	Do(thisctx)
	// if err != nil {
	// 	log.Fatal(err)
	// }

	// for _, hit := range res.Hits.Hits {
	// 	d, _ := hit.Source_.MarshalJSON()
	// 	fmt.Println(string(d))
	// 	fmt.Println("***********")
	// }

	// fmt.Println("create-mamad-index:")
	// log.Println(client.Indices.Create("mamad"))

	// client.Search().
	// 	Index("products").
	// 	Request(&search.Request{
	// 		Query: client.Query(
	// 			esdsl.NewBoolQuery().
	// 				Must(esdsl.NewMatchQuery("title", "wireless")).
	// 				Filter(esdsl.NewTermQuery("brand", esdsl.NewFieldValue().String("Samsung"))),
	// 		),
	// 	},
	// 	)

}
